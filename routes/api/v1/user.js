import express from 'express';

// const router = express.Router();
const router = express.Router();

router.get('/',);
router.post('/');
router.post('/login',);
router.get('/logout',);

export default router;